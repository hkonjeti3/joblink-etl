# Cloud Renderer (Playwright) — Cloud Run

Returns `{ status, finalUrl, html }` for `GET /render?url=...` when header `x-renderer-key` matches.
Deploy with Cloud Run and set its URL/KEY in Apps Script Script Properties.